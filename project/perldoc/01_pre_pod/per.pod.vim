" 第三行 perl => perl.pod
" 将行首空格去掉
:%s/^\s\+//
" 每行之间加一个空行
:%s/$/\r/
" 将两行空行合并成一行
:%s/^$\n\+/\r/
" 将行首文件名规范
:37,425s/^\(per\w\+\)/\1.pod/
" 将所有文件名替换成 F<> 格式
:%s/\(per\w\+\.pod\)/F<\1>/
" 将多余空格变成 冒号
:%s/pod>\s\+/pod>: /

