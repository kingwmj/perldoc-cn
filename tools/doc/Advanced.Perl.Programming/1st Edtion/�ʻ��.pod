abstract class =>抽象类
adaptor => 适配器
API(Application Programming interface) => 应用程序编程接口
array => 数组
assignment operator => 赋值运算符
bind => 联编
bitmask => 位屏蔽
block => 阻塞
callback => 回调函数
AST(Abstract Syntax Tree) => 抽象语法树
atomicity => 原子性
attribute => 属性
AWT(Abstract Windowing Toolkit) => （Java的）抽象窗口工具包
base class => 基类
binary large object(BLOB) => 二进制大对象数据类型
CASE(Computer Aided Software Engineering) => 计算机辅助软件工程（技术）
cast => 类型转换
CGI(Common Gateway Interface) => 公共网关接口
children => 子（节点，指针）
class => 类
class member => 类成员
CLI(Call Level Interface) => 调用级接口
closure => 闭包
compile-time => 编译时
composition => 组合
consistency => 一致性
constructor => 构造函数
CPAN(Comprehensive Perl Archive Network) => 综合Perl文档网络
CSV(comma-separated value) => 逗号分隔值（文件）
DBI(DataBase Interface) => 数据库接口
declaration => (函数的)声明
delegation => 代理
denial of service => 拒绝服务(攻击)
dereference => 间接引用
derived class => 派生类
destructor => 构析函数
directory handle => 目录句柄
dummy structure => 哑结构
durability => 持久性
entry => 输入条，条目
exception => 例外
executor => 执行器
file descriptor => 文件描述符
file handle => 文件句柄
garbage collection => 垃圾收集
geometry management => 布局管理
glue code => 粘连代码
GUI(Graphic User Interface）=> 图形用户界面
handle => 句柄
hash => 散列
hash reference => 散列引用
hash value => 散列值
hash variable => 散列变量
IDL(Interface Definition Language) => 接口定义语言
index => 索引
indicator => 指示器
instance => 实例
introspection => 内省
invocation => (函数的)调用
interpreter => 解释器
IPC(Interprocess Communication) => 进程间通讯
isolation => 隔离性
item => 元素
iterator => 迭代器
life-cycle => 生命周期
Linker => 链接程序
mark => 标记
memory leak => 内存泄漏
metadata => 元数据
method => 方法
MFC(Microsoft Foundation Class) => 微软基础类库
module => 模块
mutual recursion => 相互递归
namespace => 名字空间
non-blocking I/O => 非阻塞I/O
object => 对象
ODBC(Open Database Connectivity) =>  开放数据库连接（标准）
OOP(Object-Oriented Programming) => :if expand("%") == ""|browse confirm w|else|confirm w|endif
面向对象编程
opcode => 操作码
package => 包
parser => 分析器
persistence => 持续性（存储）
pipe handle => 管道句柄
POD(Plain Old Documentation) => 普通旧式文档
pointer =>  指针
polymorphism => 多态性
procedure => 过程
process => 进程
pseudo class => 伪类
RDBMS(Relational Database Management => System) 关系型数据库管理系统
reference => 引用
reflection => 反射
